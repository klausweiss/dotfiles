[user]
    email =
    name =
[core]
    autocrlf = input
    editor = vim
[push]
    default = matching
[alias]
    ignore = "!gi() { curl -L -s https://www.gitignore.io/api/$@ ;}; gi"
    new = "!git init; touch .gitignore; git add .gitignore; git commit -m 'Initial commit'"
