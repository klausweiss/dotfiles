- defaults:
    link:
        create: true
        relink: true
        force: true

- clean: ['~', '~/.config', '~/.Xresources.d']

- shell:
    - command: bash -c "git submodule update --init --recursive --depth 1 ||
                        git submodule update --recursive --depth 1"
      description: Updating submodules
