name: xmonad-config

dependencies:
  - base
  - containers
  - xmonad
  - xmonad-contrib
  - unliftio

library:
  source-dirs: .
    
executables:
  xmonad:
    main: xmonad.hs
    dependencies:
      - xmonad
      - X11 >= 1.10
