name: xmonad-config

dependencies:
  - base
  - containers
  - template-haskell
  - unliftio
  - xmonad
  - xmonad-contrib

library:
  source-dirs: .
    
executables:
  xmonad:
    main: xmonad.hs
    dependencies:
      - xmonad
      - X11 >= 1.10
